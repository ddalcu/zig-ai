diff --git a/src/core/ggml_extend.hpp b/src/core/ggml_extend.hpp
index 7dc37cb..098f2a3 100644
--- a/src/core/ggml_extend.hpp
+++ b/src/core/ggml_extend.hpp
@@ -1144,24 +1144,33 @@ __STATIC_INLINE__ ggml_tensor* ggml_ext_conv_3d(ggml_context* ctx,
                                                 int d1              = 1,
                                                 int d2              = 1,
                                                 bool force_prec_f32 = false) {
-    if (force_prec_f32) {
-        ggml_tensor* im2col = ggml_im2col_3d(ctx, w, x, IC, s0, s1, s2, p0, p1, p2, d0, d1, d2, w->type);
-
-        int64_t OC = w->ne[3] / IC;
-        int64_t N  = x->ne[3] / IC;
-        x          = ggml_mul_mat(ctx,
-                                  ggml_reshape_2d(ctx, im2col, im2col->ne[0], im2col->ne[3] * im2col->ne[2] * im2col->ne[1]),
-                                  ggml_reshape_2d(ctx, w, w->ne[0] * w->ne[1] * w->ne[2] * IC, OC));
-        ggml_mul_mat_set_prec(x, GGML_PREC_F32);
-
-        int64_t OD = im2col->ne[3] / N;
-        x          = ggml_reshape_4d(ctx, x, im2col->ne[1] * im2col->ne[2], OD, N, OC);
-        x          = ggml_cont(ctx, ggml_permute(ctx, x, 0, 1, 3, 2));
-        x          = ggml_reshape_4d(ctx, x, im2col->ne[1], im2col->ne[2], OD, OC * N);
-    } else {
-        x = ggml_conv_3d(ctx, w, x, IC, s0, s1, s2, p0, p1, p2, d0, d1, d2);
+    // zig-ai: route 3D convolution through the GGML_OP_CONV_3D op (which has a
+    // Metal kernel) instead of ggml_conv_3d / ggml_im2col_3d, which decompose
+    // into GGML_OP_IM2COL_3D — an op upstream ggml-org's Metal backend does NOT
+    // implement (it aborts with "unsupported op 'IM2COL_3D'"). This keeps Wan /
+    // LTX video generation on the GPU. The CONV_3D Metal kernel requires F16/F32
+    // weights and an F32, contiguous input.
+    const int64_t OC = w->ne[3] / IC;
+    const int64_t N  = x->ne[3] / IC;
+
+    ggml_tensor* wc = w;
+    if (wc->type != GGML_TYPE_F16 && wc->type != GGML_TYPE_F32) {
+        wc = ggml_cast(ctx, wc, force_prec_f32 ? GGML_TYPE_F32 : GGML_TYPE_F16);
+    } else if (force_prec_f32 && wc->type != GGML_TYPE_F32) {
+        wc = ggml_cast(ctx, wc, GGML_TYPE_F32);
+    }
+
+    ggml_tensor* xc = x;
+    if (xc->type != GGML_TYPE_F32) {
+        xc = ggml_cast(ctx, xc, GGML_TYPE_F32);
+    }
+    if (!ggml_is_contiguous(xc)) {
+        xc = ggml_cont(ctx, xc);
     }
 
+    x = ggml_conv_3d_direct(ctx, wc, xc, s0, s1, s2, p0, p1, p2, d0, d1, d2,
+                            (int)IC, (int)N, (int)OC);
+
     if (b != nullptr) {
         b = ggml_reshape_4d(ctx, b, 1, 1, 1, b->ne[0]);  // [OC, 1, 1, 1]
         x = ggml_add_inplace(ctx, x, b);
