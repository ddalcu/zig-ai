diff --git a/src/qwen3_tts.cpp b/src/qwen3_tts.cpp
index c2f3434..20091e9 100644
--- a/src/qwen3_tts.cpp
+++ b/src/qwen3_tts.cpp
@@ -11,6 +11,8 @@
 
 #ifdef __APPLE__
 #include <mach/mach.h>
+#elif defined(_WIN32)
+// no getrusage; the memory snapshot (diagnostic logging only) is unsupported
 #else
 #include <sys/resource.h>
 #endif
@@ -46,6 +48,9 @@ static bool get_process_memory_snapshot(process_memory_snapshot & out) {
         out.phys_footprint_bytes = out.rss_bytes;
     }
     return true;
+#elif defined(_WIN32)
+    (void) out;
+    return false;
 #else
     struct rusage usage = {};
     if (getrusage(RUSAGE_SELF, &usage) != 0) {
